import 'package:flutter/material.dart';

class AppColors {
  const AppColors._(); // prevents instantiation

  // 🌑 Main aesthetic black
  static const Color black = Color(0xFF1A1A1A);

  // ⚪ Clean white
  static const Color white = Color(0xFFFFFFFF);

  // 🔴 Aesthetic, soft premium red
  static const Color red = Color(0xFFE63946);

  static const Color gray = Color(0xFF7A7A7A);
  static const Color lightGray = Color(0xFFF0F0F0);
}
